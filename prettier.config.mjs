/** @type {import('prettier').Config} */
const config = {
	printWidth: 100,
	trailingComma: 'all',
	singleQuote: true,
	semi: false,
	plugins: ['prettier-plugin-tailwindcss'],
}

export default config
